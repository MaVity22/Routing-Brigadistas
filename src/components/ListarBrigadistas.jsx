import React from "react";
import { useNavigate} from "react-router-dom";

const ListarBrigadistas = (props) => {
    const {listadoBrigadistas} = props;
    let navigate = useNavigate();
    return (
        <div>

        </div>
    );
}

export default ListarBrigadistas;